apiVersion: v1
kind: Service
metadata:
  name: billingservice-app-api
  namespace: billingservice
spec:
  selector:
    app: billingservice-app-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP

