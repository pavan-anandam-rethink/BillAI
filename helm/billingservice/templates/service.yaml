apiVersion: v1
kind: Service
metadata:
  name: {{ include "billingservice.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: billingservice
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
