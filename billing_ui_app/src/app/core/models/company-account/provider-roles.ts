import { ProviderPermission } from './provider-roles/permissions';
import { ProviderRole } from './provider-roles/roles';

export class ProviderRoles {
  permissions: ProviderPermission[];
  roles: ProviderRole[];
}