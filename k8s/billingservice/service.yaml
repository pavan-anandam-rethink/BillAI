apiVersion: v1
kind: Service
metadata:
  name: billingservice-web
  namespace: billing
  labels:
    app.kubernetes.io/name: billingservice
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: billingservice
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
